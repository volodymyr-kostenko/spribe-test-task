spring:
  application:
    name: spribe-test-task
  jpa:
    database: "POSTGRESQL"
    show-sql: true
    hibernate:
      ddl-auto: none
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/db
    username: user
    password: password
    driverClassName: org.postgresql.Driver

integration:
  currency:
    api:
      url: "https://data.fixer.io"
      key: "667dba7b4a71147468b7f85bf1041011"
